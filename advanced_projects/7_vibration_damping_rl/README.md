# Adaptive Vibration Damping in High-Speed Rotating Machinery

## Description
A reinforcement learning policy that dynamically adjusts active magnetic bearings.

## AI Model
Using Hugging Face model: `edbeeching/decision-transformer-gym-walker2d-expert`

## Setup
Run `pip install -r requirements.txt`
Run `python main.py`
