# Smart Pneumatic Actuator Early-Stage Fault Diagnosis

## Description
Treating pressure and displacement sensor logs as 'languages' of machine behavior.

## AI Model
Using Hugging Face model: `distilbert-base-uncased`

## Setup
Run `pip install -r requirements.txt`
Run `python main.py`
